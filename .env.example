ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-opus-4-7

# Set to 1 to route /api/chat through a deterministic keyword classifier
# (src/lib/mockReporter.ts) instead of calling the LLM. Useful for demos
# and UI work without burning tokens. Leave unset for real LLM traffic.
# NEXTRADE_MOCK=1
